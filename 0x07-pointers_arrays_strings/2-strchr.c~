#include "holberton.h"
/**
 *
 *
 *
 */
char *_strchr(char *s, char c)
{
	while (*s)
	{
		if (*s == c)
		{
			return (s);
		}
		s++;

/*		if (*s == c)
			return (s);
			s++;                */
	}
	return ('\0');

/*	if (c == '\0')
		return (s);
	else
	return ('\0');      */
}
