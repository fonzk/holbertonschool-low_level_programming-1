#include <stdio.h>

/**
 * main - Entry point
 * Descriptions: Write a program that prints single digits base 10 from 0
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; ++i)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
